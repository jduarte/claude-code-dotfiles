{
  "mcpServers": {
    "github": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "-e", "GITHUB_PERSONAL_ACCESS_TOKEN", "github-mcp-local"],
      "disabled": true,
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${MCP__GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}